# Legacy Breaklist 1.x

The original single-binary report generator, kept for reference. It is not built by the root module (it has its own go.mod files). The 2.x code lives under cmd/ and internal/.
